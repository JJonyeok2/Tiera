services:
  db:
    image: postgres:16
    container_name: tiera-db
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tiera
    ports: ["5432:5432"]
    volumes: ["tiera-pgdata:/var/lib/postgresql/data"]
volumes:
  tiera-pgdata:
